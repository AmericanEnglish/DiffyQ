function runkot(init_x, init_y, fin_x, step)
% Symolic Equation
syms f(x,y)

% Acquire the differential equation
f(x, y) = sym (input('y'' = ', 's'))

% Set up k1 -> k4

% Calculate using step values

% Plot the approximations

% Plot the vector field